{
  "name": "railwayfront-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "build:cloudflare": "nuxt build --preset cloudflare",
    "build:pages": "nuxt build --preset cloudflare-pages",
    "cache:clear": "nuxi cleanup",
    "cleanup": "nuxi cleanup",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami",
    "cf:secret": "wrangler pages secret put REFRESH_SECRET --project-name railwayfront",
    "cf:deploy": "wrangler pages deploy .output/public --project-name railwayfront",
    "dev": "nuxt dev --dotenv .env.dev",
    "generate": "nuxt generate --dotenv .env.client",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "wrangler pages deploy .output/public --project-name railwayfront",
    "deploy:cloudflare": "wrangler pages deploy .output/public --project-name railwayfront",
    "deploy:clean": "npm run cache:clear && npm run build:pages && npm run deploy"
  },
  "dependencies": {
    "@glidejs/glide": "^3.7.1",
    "@nuxtjs/i18n": "^9.1.5",
    "@pinia/nuxt": "^0.11.0",
    "@vite-pwa/nuxt": "^1.1.1",
    "debounce": "^2.2.0",
    "embla-carousel-vue": "^8.5.2",
    "moment-timezone": "^0.5.47",
    "nuxt": "^3.21.7",
    "pinia": "^3.0.4",
    "vue": "latest",
    "vue-cryptojs": "^2.4.8",
    "vue-dompurify-html": "^5.3.0",
    "vue-router": "latest",
    "vue3-social-sharing": "^1.5.0"
  },
  "devDependencies": {
    "stylus": "^0.64.0",
    "wrangler": "^4.0.0"
  }
}